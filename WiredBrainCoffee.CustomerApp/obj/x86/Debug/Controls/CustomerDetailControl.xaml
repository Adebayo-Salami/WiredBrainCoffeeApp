<UserControl x:ConnectionId='1'
    x:Class="WiredBrainCoffee.CustomerApp.Controls.CustomerDetailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WiredBrainCoffee.CustomerApp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="root">

    <StackPanel>
        <TextBox Header="Firstname" Margin="10"  Text="{Binding ElementName=root, Path=Customer.Firstname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Header="Lastname" Margin="10" Text="{Binding ElementName=root, Path=Customer.Lastname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <!--<TextBox Header="Lastname" Name="txtLastName" Margin="10" TextChanged="TextBox_TextChanged" Text="{Binding ElementName=root, Path=Customer.Lastname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>-->
        <CheckBox Content="Is Developer" Margin="10" IsChecked="{Binding ElementName=root, Path=Customer.IsDeveloper, Mode=TwoWay}" />
        <!--<CheckBox Content="Is Developer" x:Name="chkIsDeveloper" Margin="10" Checked="CheckBox_IsCheckChanged"  Unchecked="CheckBox_IsCheckChanged" />-->
    </StackPanel>
</UserControl>

